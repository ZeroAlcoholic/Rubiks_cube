# 我們在做什麼？用手機拍一下，網頁幫你解魔術方塊

## 簡單說，這個網頁可以做什麼？

1. **用手機鏡頭掃描你手上的魔術方塊**（不管你怎麼把它轉亂的）
2. **網頁自動幫你分析顏色**，然後在畫面上建出一個一模一樣的 3D 虛擬方塊
3. **網頁算出怎麼解**，然後一步一步動畫示範給你看
4. **你跟著做**，把手上的真實方塊解開

就像有一個老師坐在你旁邊，他看了你的方塊之後，慢慢示範每一個動作給你看。

---

## 為什麼要做這個？

市面上有很多「自動解題」的網頁，但通常是：
- 你要自己一格一格輸入顏色（很麻煩）
- 解完就結束了，你完全不知道怎麼解的

我們想做的是：
- **用拍照代替手動輸入**（方便）
- **動畫一步一步教你做**（你自己學會）

---

## 支援哪幾種方塊？

| 方塊 | 幾乘幾 | 有幾塊貼紙 | 難度 |
|---|---|---|---|
| 三階 | 3×3×3 | 54 張 | 入門 |
| 四階 | 4×4×4 | 96 張 | 中級 |
| 五階 | 5×5×5 | 150 張 | 進階 |

每個尺寸都是一個獨立的網頁，你可以從入口頁選擇要玩哪一種。

---

## 怎麼拍？拍幾張？

### 三階方塊：可以選兩種方式

**方式一：斜角快速（2張）**

把方塊斜著拿，一次可以看到三個面。拍兩個不同角落，就能看到全部六個面。

```
第一張：拿著方塊的「前右上」角朝向鏡頭
        → 看到上面、前面、右面（共 27 格）

翻轉方塊

第二張：「後左下」角朝向鏡頭
        → 看到下面、後面、左面（共 27 格）

拍完！54 格全部掃描到了。
```

**方式二：逐面精確（6張）**

每次把一個面正對鏡頭拍一張，共拍六面。比較準確，但要多翻幾次。

### 四階、五階：同樣支援斜角 2 張

四階和五階格子比較多，但只要畫面上有輔助線框幫你對準，斜角拍同樣可行。

```
第一張：把方塊斜著拿，調整角度讓三個面都看得清楚
        網頁會畫出立體線框，你把方塊對準框框就好
        → 看到上面、前面、右面

翻轉方塊

第二張：換另一個角落朝向鏡頭
        → 看到下面、後面、左面

拍完！全部掃描到了。
```

- 四階：每張看到 3 × 16 = 48 格，兩張共 96 格
- 五階：每張看到 3 × 25 = 75 格，兩張共 150 格

如果某些格子辨識錯誤，後續的手動修色步驟可以修正。
也可以改用逐面 6 張模式，準確率更高但需要多翻幾次。

---

## 網頁怎麼「認出」顏色？

### 步驟一：取樣

每個格子的中間，取一小塊 5×5 個像素，算出平均顏色。

（直接取最中心那一點容易受到雜訊影響，取平均比較準確。）

### 步驟二：轉換色彩空間

人眼看顏色的方式跟電腦不一樣。電腦用 RGB（紅、綠、藍三個數字），但 RGB 很容易受光線影響——同一個橘色，在陰暗處看起來可能像紅色。

所以我們把 RGB 轉成 **HSV**（色相、飽和度、明度）：
- **色相（H）**：顏色是什麼（紅？綠？藍？）
- **飽和度（S）**：顏色有多鮮艷
- **明度（V）**：顏色有多亮

轉成 HSV 之後，就算光線不同，同一個顏色的「色相」範圍還是差不多，辨識準確率高很多。

### 步驟三：對照表

```
黃色：色相 45–65，飽和度 > 50%
橘色：色相 15–35，飽和度 > 60%
藍色：色相 200–240，飽和度 > 50%
白色：飽和度 < 20%，明度 > 80%
紅色：色相 0–10 或 350–360，飽和度 > 60%
綠色：色相 100–140，飽和度 > 50%
```

### 如果認錯了？

顏色有可能因為反光或光線不好被認錯。所以掃完之後，**畫面會顯示一張展開圖**，讓你對照真實方塊確認一格一格是否正確。如果哪格不對，直接點那格改顏色就好。

---

## 怎麼解題？

### 三階：有兩種解法可以選

**選項一：層先法（LBL）— 學習用**

層先法是大部分初學者學的方法，把方塊分成七個步驟依序完成，每一步都有邏輯可循，學完就真的會解：

1. **白色十字**：先在白色那面拼出一個十字
2. **白色角塊**：把白色四個角補完
3. **第二層邊塊**：把中間那層的邊塊放好
4. **頂層十字**：在黃色那面拼出十字
5. **頂層角塊方向**：讓黃色角塊的黃面朝上
6. **頂層角塊位置**：讓角塊去到正確的角落
7. **頂層邊塊**：最後把邊塊排好，完成！

總共約 80–120 步。

---

**選項二：最少步驟解法 — 電腦最優解**

這是用叫做「Kociemba 兩階段演算法」的電腦程式算出來的解法。有數學家證明，任何一個三階魔術方塊，最多只需要 **20 步**就能解開（這個數字叫做「上帝之數」）。

電腦會找出接近最短的解法（通常 20–26 步），動畫示範給你看，你跟著做就好。

注意：這個解法是電腦邏輯，動作不一定有規律，很難從中學到「公式」。適合想快速解開、不在意理解過程的人。

首次選擇這個模式時，需要從網路下載一個約 2MB 的資料檔，稍等幾秒。

### 四階：先「歸簡」，再當三階解

四階比三階多出兩種零件：
- **中心塊（每面有 4 個）**：要先把同一個面的 4 個中心塊拼在一起
- **邊塊（每條邊有 2 個）**：要先把同一條邊的 2 個邊塊配對

做完這兩件事，四階就「假裝」變成三階了，再用層先法解。

特別注意：有時候會遇到「奇偶問題（Parity）」，需要用特殊公式處理。

### 五階：同樣歸簡，但中心更複雜

五階的中心每面有 9 個，要分三條（Strip）慢慢拼；邊塊每條邊有 3 個，配對也更複雜。但五階因為是奇數層，所以沒有 PLL 那種奇偶問題，比四階少一個麻煩。

---

## 解題示範長什麼樣子？

**不是文字說明，而是直接動畫！**

每一步，畫面上的 3D 方塊就會動給你看：
- 比如這步是「右面順時針轉」，3D 方塊的右面就會慢慢轉動
- 你看著畫面，把手上的真實方塊也跟著轉
- 轉完了，點「下一步」，繼續看下一個動畫

畫面底部有一排播放控制列：

| 按鈕 | 功能 |
|---|---|
| ⏮ 上一步 | 動畫倒退一步，方塊回到上一個狀態 |
| ▶ 自動播放 | 動畫結束後自動繼續下一步（中間停頓 1.5 秒） |
| ⏸ 暫停 | 停在目前這步，不自動繼續 |
| ⏭ 下一步 | 手動推進到下一步 |

**你可以隨時在「自動」和「手動」之間切換。** 例如看到不確定的步驟就按暫停，重看動畫，確認自己做對了再繼續。

另外，在進入每個大階段（比如從「白色十字」到「白色角塊」）時，畫面會顯示「這個階段做完，你的方塊應該長這樣」的預覽，讓你確認自己有沒有做對。

---

## 視角怎麼設定？

你拍完方塊之後，3D 模型會用**和你拍照時一樣的角度**出現在畫面上。這樣你的真實方塊和畫面方向一致，不用在腦袋裡轉換。

你也可以用手指旋轉 3D 方塊（轉動範圍有限制），看完背面後，動畫播放時會自動轉回來。

---

## 這個網頁部署在哪裡？

放在 **GitHub Pages**（一個免費的網頁伺服器），網址有 `https://`。

一定要有 `https://` 才能讓手機的鏡頭運作（這是手機瀏覽器的安全規定）。

你也可以把這個網頁「加入主畫面」，就像裝了一個 APP 一樣，直接從桌面開啟，而且沒有網路也能用（因為會預先把程式存在手機裡）。

---

## 用了哪些技術？（給有興趣的人）

| 功能 | 技術 | 說明 |
|---|---|---|
| 畫 3D 方塊 | Three.js | 一個免費的 3D JavaScript 函式庫 |
| 控制 3D 視角 | OrbitControls | Three.js 的附加元件 |
| 手機鏡頭 | WebRTC | 瀏覽器內建的鏡頭功能，不用裝 APP |
| 顏色辨識 | 自己寫的 HSV 分類器 | 純 JavaScript，不需要伺服器 |
| 解題演算法（學習） | 自己寫的 LBL Solver | 純 JavaScript，不需要伺服器 |
| 解題演算法（速解） | min2phase（Kociemba） | 從網路下載一個 2MB 的資料表，最多 20 步 |
| 介面樣式 | Tailwind CSS | 讓網頁好看的工具 |

**完全不需要後端伺服器。** 所有的計算都在你的手機或電腦的瀏覽器裡執行，資料不會傳到任何地方。

---

## 開發順序（怎麼一步一步做出來）

### 第一步：把解題引擎寫好（最重要的地基）

先寫一個「給任意合法的方塊狀態，就能算出解法」的程式。

目前的舊程式只能解「從頭開始記錄每個動作」的方塊，沒有辦法解掃描進來的方塊。所以要全部重寫。

```
輸入：54 個顏色（代表 3×3 方塊的六個面）
輸出：一串動作，例如 ["U", "R'", "F2", "B", ...]
```

驗證方式：用已知的打亂公式打亂方塊，跑解題程式，應該要能還原。

---

### 第二步：修好狀態系統

現在的程式用「記錄動作歷史」來判斷方塊有沒有解開，掃描進來的方塊沒有歷史，所以沒辦法用。

要改成：「看現在六個中心格是什麼顏色，來判斷有沒有解開。」

同時要做一個對照表（Mapping Table），把「掃描到的 54 個顏色」對應到「3D 模型的 27 個小方塊的哪個面」。

---

### 第三步：做鏡頭掃描 UI

- 開啟後置鏡頭
- 畫面上疊加一個 N×N 的引導框（讓使用者知道方塊要對準哪裡）
- 即時顯示辨識到的顏色（讓使用者對準前就知道辨識結果）
- 拍照後做顏色分類

---

### 第四步：做確認和修正 UI

- 顯示 2D 展開圖（方塊展開成平面的樣子）
- 使用者可以點任何一格，把顏色改成正確的
- 系統檢查有沒有問題（比如白色出現了 10 格，應該只有 9 格）

---

### 第五步：做動畫解題引導

- 計算出解法步驟
- 一步一步播動畫
- 提供「重播」和「下一步」
- 攝影機固定在掃描時的角度
- LBL 階段之間顯示預覽

---

### 第六步：擴充到四階

複製三階的 HTML，修改：
- 方塊幾何：位置從 −1.5 到 +1.5，共 56 個小方塊
- 新增廣域轉動功能（同時轉兩層）
- 新增 Reduction 解法（先拼中心，再配邊塊）
- 掃描格子改成 4×4

---

### 第七步：擴充到五階

複製四階的 HTML，修改：
- 方塊幾何：位置從 −2 到 +2，共 98 個小方塊
- Reduction 改成 Center Strips（9 塊中心）和 Triplet Edges（3 塊邊）
- 移除 PLL Parity（五階沒有這個問題）
- 掃描格子改成 5×5

---

### 第八步：收尾和上線

- 做入口首頁（讓使用者選三階/四階/五階）
- 加上 PWA 設定（讓手機可以「安裝」這個網頁）
- 推上 GitHub Pages

---

## 最常見的問題

**Q：辨識顏色一定準嗎？**
不一定。反光、燈光顏色偏黃、方塊本身顏色偏深都會影響辨識。這就是為什麼我們加了「手動修色」的步驟，讓你自己確認和修正。

**Q：為什麼要 HTTPS？用手機直接開 HTML 不行嗎？**
手機瀏覽器的安全設定規定：只有在 `https://` 的網站上，才允許使用鏡頭。直接開 HTML 檔案（網址是 `file://`）沒有辦法存取鏡頭。

**Q：這是 APP 嗎？要去哪裡下載？**
不是 APP，是網頁。但你可以在 Safari（iOS）或 Chrome（Android）裡，把它「加入主畫面」，之後就可以像 APP 一樣從桌面開啟。

**Q：我的資料有沒有被上傳？**
沒有。所有計算都在你的裝置上執行，照片和顏色資料完全不會傳出去。
